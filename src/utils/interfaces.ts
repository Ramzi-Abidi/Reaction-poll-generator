export interface ArrowProps {
    iconType: string;
}
export type icon = {
    img: string;
    title: string;
    type: string;
};
export type iconStates = {
    likeState: string;
    loveState: string;
    supportState: string;
    curiousState: string;
    insightState: string;
};

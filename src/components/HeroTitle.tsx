const HeroTitle = () => {
    return (
        <h1 className="hero-title">
            Boost Your Audience on LinkedIn <br />
            10x Faster
        </h1>
    );
};

export default HeroTitle;
